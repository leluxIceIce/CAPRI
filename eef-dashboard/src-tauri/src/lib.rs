// Library crate target required by the Cargo.toml `[lib]` section (the
// standard Tauri v2 scaffold shape, which also enables mobile targets later
// if ever needed). The actual app is launched from `main.rs`; this stays
// empty for the spike.
